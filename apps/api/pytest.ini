[pytest]
markers =
    supabase_integration: requires a real, isolated Supabase test project (PEAK3_TEST_SUPABASE_URL etc). Skips with an explicit "not configured" reason when those secrets are absent — never silently reported as passing.
