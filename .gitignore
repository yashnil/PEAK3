# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Caches & generated data (rebuildable from source)
cache/html/
cache/processed/
results/
data/generated/
context_build.log

# User-provided optional context files (keep templates in data/examples/)
data/manual_context.csv
data/external_impact.csv
data/stat_titles.csv

.DS_Store

# NOTE: leaderboards/ and reports/ are intentionally TRACKED release artifacts
# (canonical top-250 Prime leaderboards + audit/completeness reports) and must
# NOT be added here. results/ above is local legacy scratch and stays ignored.

# App: API
apps/api/.env
apps/api/.venv/
apps/api/__pycache__/
apps/api/.pytest_cache/

# App: Web
apps/web/node_modules/
apps/web/.next/
apps/web/.env.local
apps/web/test-results/
apps/web/playwright-report/

# Web dataset (re-generated from leaderboard CSVs)
data/web/

# Ranked-readiness validation harness (Phase 4.0): generated reports and
# real reviewer data — never committed. See scripts/ranked_validation/.
scripts/ranked_validation/output/
scripts/ranked_validation/expert_review_harness/reviews.jsonl

# General
.DS_Store
*.log

# Frontend test/build artifacts
apps/web/coverage/
apps/web/blob-report/
apps/web/.turbo/
apps/web/out/

# Python tooling
.mypy_cache/
.ruff_cache/
coverage.xml
.coverage
htmlcov/
apps/web/tsconfig.tsbuildinfo
