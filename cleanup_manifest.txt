REPOSITORY CLEANUP MANIFEST
Generated during the postseason-weight upgrade (43/24/18/12/3) pass.

Scope: removed ONLY clearly obsolete, unused iteration artifacts from results/.
Preserved: all source data, required cache files, tests, current leaderboards
(results/top_250_*.csv, results/all_players.csv), README.md, METHODOLOGY.md,
outputs.txt, data/ (incl. generated context + manual_context.csv), and every
file referenced by active code.

Format: deleted file | reason deleted | replacement / current source of truth

----------------------------------------------------------------------------
results/pre_redesign_formula_diagnosis.csv
  reason: one-off diagnosis of a SUPERSEDED pre-redesign formula; not referenced
          by any active code; the model has since been fully re-derived.
  current source of truth: METHODOLOGY.md (formula derivation) + results/
          final_formula_map.{csv,txt} (live --audit-data output).

results/pre_redesign_formula_diagnosis.txt
  reason: text twin of the above; superseded, unreferenced.
  current source of truth: METHODOLOGY.md + README.md.

results/pre_final_master_redesign/            (whole directory)
    baseline_reports.csv
    top25_single_season.csv
    top25_three_year.csv
  reason: frozen snapshot from an OLD "master redesign" iteration; superseded by
          the current leaderboards and validation output; not referenced.
  current source of truth: results/top_250_single_seasons_{prime,performance}.csv,
          results/top_250_three_year_peaks_{prime,performance}.csv, outputs.txt.

results/shaq_peak.json
  reason: stale one-off JSON export from very early manual testing; not
          referenced; not part of any current report.
  current source of truth: `python peak3.py --player "Shaquille O'Neal" --export
          <path>` regenerates an up-to-date export on demand.

results/baseline/candidates_before.csv
  reason: pre-enrichment manual snapshot of an early candidate list; unreferenced
          and superseded by the tiered candidate build.
  current source of truth: data/generated/candidates.csv (+ candidate_exclusions.csv).

results/baseline/peak_scores_before.csv
  reason: ad-hoc "before" peak-score dump from an early iteration; unreferenced.
  current source of truth: cache/processed/scored_1980_2026.parquet + outputs.txt.

results/baseline/lebron_legacy.txt
  reason: early manual single-player report capture; unreferenced, superseded by
          the live report and outputs.txt.
  current source of truth: `python peak3.py --player "LeBron James"`.

results/baseline/wade_legacy.txt
  reason: early manual single-player report capture; unreferenced.
  current source of truth: `python peak3.py --player "Dwyane Wade"`.

----------------------------------------------------------------------------
KEPT (deliberately NOT deleted, though they look incidental):

results/baseline/scored_pre_context.parquet
  kept because peak3.py (--audit-context) references it as the before/after
  baseline snapshot and recreates it only when absent.

results/curry_2015_vs_2016_audit.{csv,txt}
  kept: current --compare-seasons output (regenerated by cli_commands).

results/score_calibration.{csv,txt}, results/scoring_dependency_audit.{csv,txt},
results/final_data_audit.*, results/final_formula_map.*, results/context_audit.*,
results/candidate_audit.*, results/teammate_audit.*, results/spot_check_validation.*,
results/all_players.csv, results/top_250_*.csv
  kept: live outputs of active --audit-* / leaderboard commands and/or referenced
  by code comments as the documented source of truth.
