REPOSITORY CLEANUP MANIFEST
Pass: recognition-weight update (Recognition 15% -> 20%; weights 38/21/20/18/3).

Method: re-ran the unused-file audit. For every results/ artifact, grep across
peak3.py, nba_peak/, make_outputs.py, tests/, README.md and METHODOLOGY.md, and
classify each reference as a WRITE site (a command that regenerates the file) vs
a READ/consume (code or docs that depend on it). A file is removable only when
it has NO read/consume reference, is not a current deliverable, and is stale
relative to the live model.

Columns: deleted path | why obsolete | evidence it is unused | current replacement.

============================================================================
DELETED THIS PASS (stale, write-only audit snapshots from old-model runs)

results/all_players.csv
  why obsolete: a full player dump generated 2026-06-12 under a superseded model.
  evidence unused: only WRITTEN by `--list-all-players` (nba_peak/cli_commands.py,
    peak3.py); no code reads it; not referenced by README/METHODOLOGY.
  current replacement: `python peak3.py --list-all-players` regenerates it on
    demand from the live cache.

results/candidate_audit.csv
results/candidate_audit.txt
  why obsolete: stale `--audit-candidates` output (2026-06-12).
  evidence unused: only WRITTEN by cli_commands.audit_candidates; no reads; not
    in docs.
  current replacement: `python peak3.py --audit-candidates`.

results/teammate_audit.csv
results/teammate_audit.txt
  why obsolete: stale `--audit-teammates` dump (2026-06-13).
  evidence unused: only WRITTEN by cli_commands.audit_teammates; no reads; not
    in docs.
  current replacement: `python peak3.py --audit-teammates`.

results/final_data_audit.json
results/final_data_audit.txt
results/final_data_coverage.csv
results/final_data_anomalies.csv
results/final_formula_map.csv
results/final_formula_map.txt
  why obsolete: stale `--audit-data` outputs (2026-06-13) describing an old-model
    formula/coverage map; the formula has since changed (weights 38/21/20/18/3).
  evidence unused: only WRITTEN by nba_peak/data_audit.py; no code reads them; not
    referenced by README/METHODOLOGY (the live formula reference is METHODOLOGY.md
    + README.md).
  current replacement: `python peak3.py --audit-data` regenerates the full set;
    the authoritative formula map is METHODOLOGY.md / README.md.

============================================================================
REVIEWED BUT PRESERVED (with reason)

results/top_250_single_seasons_{prime,performance}.csv
results/top_250_3_year_peaks_{prime,performance}.csv
results/top_250_5_year_peaks_{prime,performance}.csv
  CURRENT leaderboard deliverables, rebuilt every run from the live cache by
  make_outputs.write_leaderboards() (single-season, 3-year, 5-year; Prime and
  Performance-Only).

results/context_audit.{json,txt}
  Referenced by METHODOLOGY.md; live `--audit-context` output.

results/scoring_dependency_audit.{csv,txt}
  Referenced by a peak3.py comment AND METHODOLOGY.md (documents the still-current
  legacy `_pct` column-naming limitation).

results/baseline/scored_pre_context.parquet
  READ by peak3.py (--audit-context) as the before/after baseline; recreated only
  when absent. (Its sibling snapshots were removed in an earlier pass.)

Source / data / model files (peak3.py, make_outputs.py, nba_peak/**, tests/**,
cache/processed/*.parquet, cache/html/**, data/**, README.md, METHODOLOGY.md,
outputs.txt, requirements.txt) were all preserved.

============================================================================
PRIOR-PASS DELETIONS (already gone; see git history)
  results/pre_redesign_formula_diagnosis.*, results/pre_final_master_redesign/,
  results/shaq_peak.json, results/baseline/{lebron_legacy,wade_legacy,
  candidates_before,peak_scores_before}.*, results/spot_check_validation.*,
  results/score_calibration.*, results/curry_2015_vs_2016_audit.*,
  results/top_250_three_year_peaks_{prime,performance}.csv.

============================================================================
DATA-COMPLETENESS PASS DELETIONS (2025-06)
  _prefetch_awards.py
    purpose      : one-off helper that warmed the awards-page HTML cache.
    why obsolete : superseded by `python peak3.py --rebuild-data`, which fetches
                   + caches awards (and per-game) HTML deterministically.
    evidence     : grep of peak3.py / make_outputs.py / nba_peak/** / tests/**
                   shows zero references; never committed (untracked temp script).
    replacement  : peak3.cmd_rebuild_data (fetch_html loop).
  _rescore.py
    purpose      : ad-hoc harness to re-score from cached raw frames during dev.
    why obsolete : identical behavior to `python peak3.py --rebuild --no-scrape`
                   (proven bit-identical; see test_canonical_rebuild_matches_fast_path).
    evidence     : zero references in source/tests; untracked temp script.
    replacement  : peak3.py --rebuild --no-scrape  (offline fast path).

KEPT (verified still referenced; NOT deleted):
  data/old_model_snapshot.csv         -> make_outputs.postseason_before_after
  data/refine_pre_snapshot.csv        -> make_outputs.refinement_before_after
  data/datacomplete_pre_snapshot.csv  -> make_outputs + tests (data-completion B/A)
  results/baseline/scored_pre_context.parquet -> peak3.py / nba_peak/audit.py
  data/generated/{team_shares,mvp_votes,dpoy_votes}.csv -> canonical inputs
    (raw source evidence; rebuildable via --rebuild-data; required for provenance).
